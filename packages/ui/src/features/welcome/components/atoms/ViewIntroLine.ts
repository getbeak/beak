import styled from 'styled-components';

const ViewIntroLine = styled.p`
	font-size: 15px;
	margin-top: 0px;
	margin-bottom: 25px;
	color: var(--beak-colors-fg-muted);
`;

export default ViewIntroLine;
