import React from 'react';

import OnboardingHome from '../features/onboarding/components/OnboardingHome';

const Onboarding: React.FunctionComponent = () => (
	<OnboardingHome />
);

export default Onboarding;
