{
  "name": "@beak/common-host",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
    "compile": "tsc -b tsconfig.json",
    "typecheck": "tsc",
    "start": "tsc -b tsconfig.json -w"
  },
  "dependencies": {
    "@beak/common": "*",
    "@beak/ksuid": "*",
    "@getbeak/types": "^2.1.0",
    "@getbeak/types-variables": "^2.1.0"
  }
}
