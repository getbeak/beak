{
	"name": "@beak/requester-node",
	"version": "0.0.0",
	"private": true,
	"main": "dist/index",
	"types": "dist/index",
	"files": [
		"dist"
	],
	"scripts": {
		"build": "pnpm clean && pnpm compile",
		"clean": "rm -rf ./dist && rm -rf tsconfig.tsbuildinfo",
		"compile": "tsgo --build tsconfig.json",
		"typecheck": "tsgo",
		"lint": "biome lint .",
		"watch": "tsgo --build tsconfig.json --watch",
		"test": "vitest",
		"test:run": "vitest run --passWithNoTests"
	},
	"dependencies": {
		"@beak/common": "*",
		"@getbeak/types": "^2.1.0",
		"node-fetch": "^3.3.2"
	},
	"devDependencies": {
		"@types/node-fetch": "^2.6.13",
		"vitest": "^4.1.6"
	}
}
